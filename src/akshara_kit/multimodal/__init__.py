"""Multimodal vision-language fallback — see :mod:`.fallback`.

Opt-in only: nothing here contacts a provider unless the caller passes a
:class:`~akshara_kit.contracts.extraction.MultimodalConfig`.
"""
